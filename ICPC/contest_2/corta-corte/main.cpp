#include<iostream>
#include<vector>

using namespace std;

int main()
{
	int n, a, b, c;

	cin >> n >> a >> b >> c;

	for(int k = 0; k <= n; k++)
	{
		for(int l = 0; l <= n; l++)
		{
			/*
			for(int m = 0; m <= n; m++)
			{
				if((k * a) + (l * b) + (m * c) == n)
				{
					cout << k * a << " " << l * b << " " << m * c << "\n";
				}
			}
			*/
			if((k * a) + (l * b) )
		}
	}

	return 0;
}